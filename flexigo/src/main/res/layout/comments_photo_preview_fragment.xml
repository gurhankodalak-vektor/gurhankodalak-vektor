<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.vektortelekom.android.vservice.ui.comments.CommentsViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorBlack"
        android:clickable="true"
        tools:ignore="KeyboardInaccessibleWidget">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/image_view_preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/image_view_close"
            android:layout_width="56dp"
            android:layout_height="56dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="20dp"
            android:padding="10dp"
            android:background="@null"
            android:tint="@color/colorWhite"
            android:src="@drawable/ic_close"
            android:onClick="@{viewModel.navigator::closePhotoPreviewFragment}"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_submit"
            style="@style/PrimaryButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toTopOf="@id/button_select_demand"
            android:layout_margin="20dp"
            android:text="@string/upload_photo"
            android:onClick="@{viewModel.navigator::useTakenPhoto}"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_select_demand"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:layout_marginBottom="16dp"
            android:textSize="14sp"
            android:textColor="@color/colorWhite"
            android:textAppearance="@style/TextRegular"
            android:text="@string/take_again"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:rippleColor="@color/colorPrimary"
            android:onClick="@{viewModel.navigator::takePhotoAgain}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>